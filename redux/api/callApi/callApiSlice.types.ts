export interface ICallInfo {
    callChannelId: string;
    sender: string;
    reciver: string;
}

export interface ITokenInfo {
    channelName: string;
    role: string;
    uid: string;
    tokentype: string;
}